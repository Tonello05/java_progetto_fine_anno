AssetSetter
        gp.npc[15] = new NPC_doganiere(gp);
        gp.npc[15].worldX = 70 * gp.tileSize;
        gp.npc[15].worldY = 95 * gp.tileSize;

        gp.npc[16] = new NPC_guardia(gp);
        gp.npc[16].worldX = 70 * gp.tileSize;
        gp.npc[16].worldY = 93 * gp.tileSize;
NPC_guardia
        package entity;

        import java.util.Random;

        import javax.imageio.ImageIO;

        import Main.GamePanel;

        public class NPC_guardia extends Entity{

            public NPC_guardia(GamePanel gp){
                super(gp);

                direction = "up"; //direzione iniziale dell'npc
                speed = 1;      //velocità dell'npc
                getPlayerImage();   
                setDialogue();
                name = "guardia";
                type = 1;   //tipo di entity ( 1 = npc)
                defaultDirection = "up";
                noMovement = true;
            }

            public void getPlayerImage(){       //legge le immagini del player

                try {
                    
                    down1 = ImageIO.read(getClass().getResourceAsStream("/res/npc/guardia/guardia0.png"));
                    left1 = ImageIO.read(getClass().getResourceAsStream("/res/npc/guardia/guardia2.png"));
                    right1 = ImageIO.read(getClass().getResourceAsStream("/res/npc/guardia/guardia1.png"));

                } catch (Exception e) {
                    e.printStackTrace();
                }
            }

            
            public void setAction(){    //esegue un azione (per questo npc si limita a  farlo muovere a caso)

                if(onPath){
                    
                    int goalCol = 30;
                    int goalRow = 8;
                    searchPath(goalCol, goalRow);

                }else if (!noMovement){
                    actionLockCounter ++;   //aumenta di 1 ogni frame

                    if(actionLockCounter == 120){   //ogni 120 frame cambia la direzione in cui sta camminando
            
                        Random random = new Random();   
                        int i = random.nextInt(100)+1; //numero a caso tra 1 a 100
            
                        if( i <= 25 ){  //seleziona una direzione a caso
                            direction = "up";
                        }else if( i > 25 && i <=50 ){
                            direction = "down";
                        }else if( i > 50 && i <=75 ){
                            direction = "right";
                        }else if(i > 75){
                            direction = "left";
                        }
            
                        actionLockCounter = 0;
            
                    }
                }
            }

            public void setDialogue(){      //dialoghi dell'npc

                dialogues[0] = "...";
                dialogues[1] = "....";
                dialogues[2] = ".....";
                dialogues[3] = "......";
                dialogues[4] = ".......?";
            }

            public void speak(){    //dialogo con l'npc

                //eventuali azioni da fare durante il dialogo (per esempio curare a un certo dialogo)

                //DIALOGO
                super.speak();
            }

        }

NPC_guardia e NPC_doganiere 

        aggiungere gli sprite di lato


OBJ_fire_sword

    volendo negli ultimi minuti di lezione ho scritto una descriozione
    "\nla spada piu' forte\n(attenzione!, questa spada\n potrebbe causare ustioni\ne colpi di calore al portatore)"

OBJ_shoes

    per qualche motivo non ti danno speed in piu

AGGIUNGERE I DIALOGHI

    a miotto,abu,agazzi e cuconato

PROVARE

    a mettere il fatto che il NPC_doganiere ti chieda i soldi per passare (magari con un item invisibile)
    a mettere NPC nelle case vuote (anche roba a caso)